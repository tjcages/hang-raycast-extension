{
  "name": "hang-backend",
  "version": "1.0.0",
  "description": "Backend API for Hang Raycast extension",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy src/index.ts --env=\"\"",
    "deploy:production": "wrangler deploy src/index.ts --env=production",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241106.0"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "wrangler": "^4.54.0"
  }
}
